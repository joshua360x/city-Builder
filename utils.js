export function createCountString(cityCount, skylineCount, castleCount) {

    return (`You have changed the city ${cityCount} times, the skyline ${skylineCount} times, and the castle ${castleCount} times`);

}